버블: 거품 // 방울
> 가볍다 > 위로
> 보통의 일반적인 경우에는 대부분 이벤트 버블링
> event.target => article (*실제 이벤트가 발생된 지점)
> event.currentTarget => (*실제 이벤트 발생 후 이벤트 경유되고 있는 지점)

> 이벤트의 성격에 따라서 버블링을 적용하지 않는다.
- mouseEnter (*이벤트 버블링)
- mouseEnter (*이벤트 버블링)
